<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Flores Amarillas</title>
  <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
  <style>
    body {
      margin: 0;
      padding: 0;
      background: linear-gradient(to bottom, #1b1b2f, #2e2e4f);
      font-family: 'Press Start 2P', cursive;
      color: #fff;
      text-align: center;
      overflow: hidden;
    }

    h1 {
      margin-top: 20vh;
      font-size: 14px;
      line-height: 1.8;
      padding: 0 20px;
      text-shadow: 2px 2px 0 #000;
    }

    button {
      margin-top: 30px;
      padding: 15px 25px;
      font-family: 'Press Start 2P', cursive;
      font-size: 12px;
      color: #1e1e2f;
      background: #ffdd00;
      border: 3px solid #fff;
      cursor: pointer;
      box-shadow: 4px 4px 0px #000;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    button:hover {
      transform: scale(1.05);
      box-shadow: 2px 2px 0px #000;
    }

    .flower {
      position: absolute;
      width: 20px;
      height: 20px;
      background: radial-gradient(circle, #ffeb3b 40%, #fbc02d 100%);
      border-radius: 50%;
      box-shadow:
        -10px -10px 0 #ffeb3b,
        10px -10px 0 #ffeb3b,
        -10px 10px 0 #ffeb3b,
        10px 10px 0 #ffeb3b;
      opacity: 0;
      animation: float 6s linear infinite, fadeIn 1s forwards;
    }

    @keyframes float {
      from {
        transform: translateY(100vh) rotate(0deg);
        opacity: 1;
      }
      to {
        transform: translateY(-20vh) rotate(360deg);
        opacity: 0.6;
      }
    }

    @keyframes fadeIn {
      to { opacity: 1; }
    }

    .final-message {
      display: none;
      margin-top: 20px;
      font-size: 12px;
      color: #ffdd00;
      text-shadow: 2px 2px 0 #000;
    }
  </style>
</head>
<body>
  <h1>Sé que este día se dan flores amarillas a personas importantes,<br>y encabezando la lista siempre irás tú, Andrein 💛</h1>
  <button id="openBtn">Abrir cajita UwU</button>
  <div class="final-message" id="finalMessage">Una flor para la flor más bonita 🌼</div>

  <script>
    const btn = document.getElementById("openBtn");
    const finalMessage = document.getElementById("finalMessage");

    btn.addEventListener("click", () => {
      btn.style.display = "none";

      // generar flores animadas
      for (let i = 0; i < 50; i++) {
        const flower = document.createElement("div");
        flower.classList.add("flower");
        document.body.appendChild(flower);

        flower.style.left = Math.random() * window.innerWidth + "px";
        flower.style.top = window.innerHeight + "px";
        flower.style.animationDuration = (4 + Math.random() * 4) + "s";
        flower.style.animationDelay = Math.random() * 2 + "s";

        setTimeout(() => flower.remove(), 10000);
      }

      // mostrar mensaje después
      setTimeout(() => {
        finalMessage.style.display = "block";
      }, 4000);

      // seguir generando flores para animación infinita
      setInterval(() => {
        const flower = document.createElement("div");
        flower.classList.add("flower");
        document.body.appendChild(flower);

        flower.style.left = Math.random() * window.innerWidth + "px";
        flower.style.top = window.innerHeight + "px";
        flower.style.animationDuration = (4 + Math.random() * 4) + "s";
        flower.style.animationDelay = Math.random() * 2 + "s";

        setTimeout(() => flower.remove(), 10000);
      }, 600);
    });
  </script>
</body>
</html>
